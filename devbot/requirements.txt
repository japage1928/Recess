# DevBot Phase 3 — Python dependencies
# Install with: pip install -r requirements.txt

# Server
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# HTTP client (Ollama + Claude API calls)
httpx==0.27.2

# Vector database — local, no server needed
chromadb==0.5.23
